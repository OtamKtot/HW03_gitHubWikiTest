1. Есть ли разница между $("h1 div"); и $("h1").$("div"); - может ли привести к тому что, поиск найдёт разные элементы?
Если может - приведите пример, когда.

- Разница есть:
$("h1 div") - ищется дочерний div родительского h1, в связке;
$("h1").$("div") - ищется первый div, лежащий в h1, при этом связь не обязательна.